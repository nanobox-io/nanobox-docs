---
title: up
---

The `up` command runs a [*create*](/cli/create/) and a [*deploy*](/cli/deploy/).

### Usage
```shell
$ nanobox up [-w]
```

### Options
#### -w, --watch
Watches your app for file changes.